---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "neon_branch_neon_auth_oauth_provider Ephemeral Resource - neon"
subcategory: ""
description: |-
  Retrieves information about a NeonAuth OAuth provider on a branch. The sensitive fields (client_id, client_secret) are ephemeral and will not be stored in Terraform state.
---

# neon_branch_neon_auth_oauth_provider (Ephemeral Resource)

Retrieves information about a NeonAuth OAuth provider on a branch. The sensitive fields (client_id, client_secret) are ephemeral and will not be stored in Terraform state.

## Example Usage

```terraform
ephemeral "neon_branch_neon_auth_oauth_provider" "example" {
  project_id = neon_project.example.id
  branch_id  = neon_branch.example.id
  id         = "google"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `branch_id` (String) The Neon branch ID.
- `id` (String) The OAuth provider ID (e.g. `google`, `github`, `microsoft`, `vercel`).
- `project_id` (String) The Neon project ID.

### Read-Only

- `client_id` (String) The OAuth client ID.
- `client_secret` (String, Sensitive) The OAuth client secret.
- `type` (String) The OAuth provider type (e.g. `standard`, `shared`).
